# AI 配置
AI_BASE_URL=https://open.bigmodel.cn/api/paas/v4
AI_API_KEY=your_key_here
AI_MODEL=glm-4
# AI 大模型并发调用上限（进程内全局共享，默认 4；如大模型后端有限流可下调）
AI_MAX_CONCURRENCY=4
# CYQ 筹码分布自动注入（调用 AI 时自动查 tushare 筹码并拼入 prompt；0 关闭）
CYQ_INJECT=1
# Tushare token（筹码查询必填）
TUSHARE_TOKEN=
# Tushare API 地址（可选，默认 http://api.tushare.pro；可指向第三方代理/网关）
TUSHARE_API_URL=

# 通知配置 - Telegram
NOTIFY_TELEGRAM_BOT_TOKEN=your_bot_token
NOTIFY_TELEGRAM_CHAT_ID=your_chat_id

# 代理配置（国内访问 Telegram 可能需要）
# HTTP_PROXY=http://127.0.0.1:7890

# SSL 企业证书（Zscaler 等企业代理环境需要，留空则不启用）
# CA_CERT_FILE=/path/to/your/enterprise-cert.crt

# 调度配置（cron 表达式：分 时 日 月 周）
DAILY_REPORT_CRON=30 15 * * 1-5
